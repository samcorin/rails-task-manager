<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "Buy milk" %>

  <%= f.label :description %>
  <%= f.text_field :description, placeholder: "I have to..." %>

  <%= f.submit %>
<% end %>
